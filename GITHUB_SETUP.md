# Инструкция по загрузке проекта на GitHub

## Шаг 1: Инициализация Git репозитория

```bash
git init
```

## Шаг 2: Добавление всех файлов

```bash
git add .
```

## Шаг 3: Создание первого коммита

```bash
git commit -m "Initial commit: Jenkins CI/CD practice project"
```

## Шаг 4: Создание репозитория на GitHub

1. Откройте [GitHub.com](https://github.com) в браузере
2. Войдите в свой аккаунт (или создайте новый)
3. Нажмите кнопку **"+"** в правом верхнем углу
4. Выберите **"New repository"**
5. Заполните форму:
   - **Repository name**: `jenkins-ci-cd-practice` (или любое другое имя)
   - **Description**: `Node.js test project for Jenkins CI/CD practice`
   - **Visibility**: выберите Public (публичный) или Private (приватный)
   - **НЕ добавляйте** README, .gitignore или license (они уже есть)
6. Нажмите **"Create repository"**

## Шаг 5: Связывание локального репозитория с GitHub

GitHub покажет вам команды. Используйте эти команды (замените `YOUR_USERNAME` на ваше имя пользователя GitHub):

```bash
git remote add origin https://github.com/YOUR_USERNAME/jenkins-ci-cd-practice.git
```

## Шаг 6: Переименование основной ветки в main (если нужно)

```bash
git branch -M main
```

## Шаг 7: Отправка проекта на GitHub

```bash
git push -u origin main
```

Вас попросят ввести логин и пароль (или Personal Access Token).

---

## Альтернативный способ: Использование GitHub CLI

Если у вас установлен GitHub CLI, можете использовать:

```bash
gh repo create jenkins-ci-cd-practice --public --source=. --remote=origin --push
```

---

## Проблемы с аутентификацией?

GitHub больше не принимает пароли. Используйте **Personal Access Token**:

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите права: `repo` (полный доступ к репозиториям)
4. Скопируйте токен
5. Используйте токен вместо пароля при `git push`

---

## Проверка

После успешной загрузки откройте ваш репозиторий на GitHub:
```
https://github.com/YOUR_USERNAME/jenkins-ci-cd-practice
```

Все файлы должны быть там! 🎉

